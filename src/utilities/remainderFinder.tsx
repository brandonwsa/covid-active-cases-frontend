/**
 * Finds the remainder of the number passed in along with the number it is to be divded by.
 * @param numerator Number you would like to find remainder of.
 * @param denominator number to divide the number by.
 */

const findRemainder = (numerator: number, denominator: number) => {
    return numerator % denominator;
}

export default findRemainder;